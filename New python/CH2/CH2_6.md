# CH2_6
```
from array import *

x= array('i',[20,17,24,13,48])
n=x.pop()
print("刪除",n)
for data in x:
  print(data)


n=x.pop(2)
print("delete",n)
for data in x:
  print(data)

pop()
會刪除最後一個元素
pop(0)
會刪除第0個元素
依此類推
```
